MONGODB_URI=mongodb+srv://alnaqeeb:Naqeeb2025@billscluster.xwtpihw.mongodb.net/BillsDB?retryWrites=true&w=majority&appName=BillsCluster
**الإجراء المطلوب:**
1.  **أنشئ** ملف `.env` داخل مجلد `backend`.
2.  **انسخ** السطر الوحيد من الكود أعلاه والصقه في ملف `.env` الذي أنشأته.
3.  **احفظ** الملف.

---

### **الاختبار الحقيقي**

الآن بعد أن أصبح كل شيء في مكانه، حان وقت الاختبار الأهم:
1.  افتح موجه الأوامر (CMD) داخل مجلد `backend`.
2.  **أوقف الخادم القديم** إذا كان لا يزال يعمل (بالضغط على `Ctrl + C`).
3.  شغل الخادم مرة أخرى بالأمر:
    ```bash
    node server.js
    ```

إذا كان كل شيء صحيحًا، يجب أن ترى رسالتين في موجه الأوامر:
تم الاتصال بقاعدة بيانات MongoDB Atlas بنجاح!Server is running on port 3000